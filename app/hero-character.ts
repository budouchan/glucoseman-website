/**
 * The character shown in the first view.
 *
 * The concept calls for the finished, full-colour Glucoseman. The artwork
 * bundled in this repository (`public/glucoseman.png`) is the uncoloured line
 * version that the previous design filled with purple, so it is only a
 * placeholder here.
 *
 * To ship the finished form: commit the colour artwork under `public/` and
 * point `src`, `width` and `height` at it. Nothing else needs to change.
 */
export const HERO_CHARACTER = {
  src: "/glucoseman.png",
  width: 1023,
  height: 1537,
  alt: "グルコースマン",
};
